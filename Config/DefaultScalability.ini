[AntiAliasingQuality@3]
r.PostProcessAAQuality=3                      // Default: 4. Lowers the TemporalAntiAliasing(TAA) quality slightly for a significant performance optimisation.

[EffectsQuality@3]
r.SSGI.Quality=2                              // Default: 3. Lowers the ScreenSpaceGlobalIllumination(SSGI) quality for a significant performance optimisation.
